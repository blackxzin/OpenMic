org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# opus_flutter_android declares its own module minSdkVersion=19, which the
# NDK bundled with current Android Gradle Plugin refuses to build for
# (requires >=21). The app itself targets minSdk 23 (see app/build.gradle.kts)
# so this only suppresses a check on a dependency's unrelated legacy setting.
android.ndk.suppressMinSdkVersionError=21
