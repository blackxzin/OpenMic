"""OpenMic desktop app package."""

# Single source of truth for the version stamped into packages (.deb, AUR)
# and reported in bug reports. Bump it in the same commit as the release tag.
__version__ = "0.1.0"
